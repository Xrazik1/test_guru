<%= form_with model: resources.values, local: true do |form| %>
  <%= render 'shared/errors', resource: resources[:question] %>

  <p>
    <%= form.label :body, 'Содержимое вопроса' %>
    <%= form.text_field :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
